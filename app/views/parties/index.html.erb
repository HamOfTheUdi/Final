<p style="color: green"><%= notice %></p>

<% content_for :title, "Parties" %>

<h1>Parties</h1>

<div id="parties">
  <% @parties.each do |party| %>
    <%= render party %>
    <p>
      <%= link_to "Show this partie", party %>
    </p>
  <% end %>
</div>

<%= link_to "New partie", new_party_path %>
